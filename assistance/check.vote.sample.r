check.vote.sample <- function(population.dist,candidate.names=NULL,sample.dist,loop.number){
  if (length(population.dist) != length(sample.dist)){
    print("sample distribution size shoud be identical to the population candidate number")
    return(-1)
  }
  
  
  
  
}

check.vote.sample(c(34,35,23),sample.dist = c(1,1),loop.number = 23)
